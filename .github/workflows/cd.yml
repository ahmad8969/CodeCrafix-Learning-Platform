name: CD Placeholder

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build production images
        run: |
          docker compose -f docker-compose.yml -f docker-compose.prod.yml build
      - name: Deploy placeholder
        run: |
          echo "Configure registry login and host deploy commands here."
          echo "Suggested targets: Render/Railway API + Vercel/Nginx web."
